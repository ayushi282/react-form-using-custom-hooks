import { createStore } from "react-redux";
// after making a store we will then make a reducer function which hgas states and actions in it  
// action has 2 states 
const reducerfun=(state={counter:0},action)=>{

};

const store = createStore(reducerfun)//a store to keep all the data in one place 